[package]
name = "fuso-linux"
version = "0.1.0"
edition = "2021"
description = "Fuso — track your team's timezones (Linux desktop app)"
license = "MIT"

[dependencies]
fuso-core = { path = "../crates/fuso-core" }
gtk4 = "0.9"
chrono = "0.4"
chrono-tz = "0.10"
glib = "0.20"
